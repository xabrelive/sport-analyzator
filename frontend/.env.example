# Куда проксировать запросы /api/* (только для сервера Next.js при npm run dev).
# Если бэкенд запущен на другом порту — укажите его:
# BACKEND_PORT=8001
# или полный URL:
# BACKEND_URL=http://localhost:8001
#
# По умолчанию используется http://localhost:8000
